
# Add install twmn to i3 install script

   git clone https://github.com/sboli/twmn.git
   sudo apt-get update
   sudo apt-get install libboost-all-dev qt5-default libqt5x11extras5-dev

   qmake
   make
   sudo make install

   add 'exec /?always /usr/local/bin/twmnd' to i3 config
   e.g.
      /usr/local/bin/twmnc -t Title -c "Battery has been disconnected"  --fg white -s 50 -i /usr/share/icons/gnome/32x32/status/error.png

# Create notyfication
   * Battery charging has been disabled
   * Brightness has been changed <value>
   * Volume has been changed <value>

# Install and configure xbacklight

# Background and border color for the individual blocks in i3bar

# Way to install external programs from git etc.
